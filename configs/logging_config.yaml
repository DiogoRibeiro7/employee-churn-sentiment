# Logging configuration consumed by employee_churn.config.configure_logging.
logging:
  level: INFO        # DEBUG | INFO | WARNING | ERROR | CRITICAL
  format: "%(asctime)s | %(levelname)-8s | %(name)s | %(message)s"
  datefmt: "%Y-%m-%dT%H:%M:%S"
